<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MunuProductionActivity">

    <!-- FRAMELAYOT PARA MOSTRAR EL CONTENIDO DEL FRAGMENTO SEGUN EL ITEM SELECCIONADO  -->
    <FrameLayout
        android:id="@+id/fragmnetContainer"
        android:layout_width="match_parent"
        android:layout_height="744dp"
        android:layout_marginStart="1dp"
        android:layout_marginEnd="1dp"
        android:background="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!--TOOBAR -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="0dp"
            android:background="@color/rosa_acento"
            android:minHeight="?attr/actionBarSize"
            app:iconTint="@color/rosa_pastel"
            app:itemIconTint="@color/white"
            app:itemTextColor="@color/rosa_acento"
            app:popupTheme="@style/toolbarDesplegable1"
            app:title="Pasteleria PMD"
            app:titleTextAppearance="@style/ToolbarTitleText"
            app:titleTextColor="@color/crema_suave" />
    </FrameLayout>

    <!-- CARD PARA EL BUTON NAVIGATION-->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!--BUTON NAVIGATION -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomnavigationProduction"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/rosa_acento"
            app:itemIconTint="@color/white"
            app:itemTextColor="@color/white"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/menu_production"
            app:itemBackground="@drawable/selector_menu"
            app:itemPaddingTop="8dp"
            app:itemPaddingBottom="8dp"
            app:itemIconSize="24dp" />
    </androidx.cardview.widget.CardView>
</androidx.constraintlayout.widget.ConstraintLayout>
